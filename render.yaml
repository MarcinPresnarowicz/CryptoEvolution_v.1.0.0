services:
  - type: web
    name: kryptoewolucja
    env: python
    region: frankfurt
    plan: standard
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    autoDeploy: true
